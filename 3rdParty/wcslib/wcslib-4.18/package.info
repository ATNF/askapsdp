# The list of libraries this package provides
# wcslib provide a library called libwcs not libwcslib
# as of 4.7 wcslib provides pkgconfig files which set
# incdir=include/wcslib. All our code uses wcslib/wcs.h though
# so use explicit  package.info here to overwrite this behaviour
libs=wcs
